1. icons shouldnt be in categories really. just add a search bar.
	 but defining them at the least is a must so that theyre curated not 5000 icons
2. padding of header of transactions modalbottomsheet different from padding content. should be configuratble from props

settings:
- about

search box should open up search and filter modal
either popping up a rect from the navbar or making the navbar have search

future:

1. add currencies which display symbol to the right
2. use the library i used in typstry to automatically get the db path

bugs:
- when grabbing drag handle down on account selector from transactions, there's sometimes a visual bug
- forbid transferring money if that would make bank balance negative
- 